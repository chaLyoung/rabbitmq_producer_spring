server:
  port: 7676
  servlet:
    context-path: /
  shutdown: graceful
  http:
    port: 7676

spring:
  config:
    activate:
      on-profile: local
  ############################################################
  # DataSource 설정
  ############################################################

  ############################################################
  # Rabbitmq 설정
  ############################################################
  rabbitmq:
    host: host here
    port: port here
    username: name here
    password: password here
    virtual-host: vhost here
    ssl:
      enabled: true

#  session:
#    store-type: jdbc
#    jdbc:
#      initialize-schema: always

#  ############################################################
#  # Restful Client Configuration
#  ############################################################
#  restTemplate:
#    factory:
#      readTimeout: 60000
#      connectTimeout: 60000
#    httpClient:
#      maxConnTotal: 100
#      maxConnPerRoute: 5